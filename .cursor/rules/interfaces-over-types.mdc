---
description:
globs:
alwaysApply: true
---

# Your rule content

Any type declarations should be with `interface` keyword, not `type`.
Correct example:
`export interface SomeComponentProps { ... }`
